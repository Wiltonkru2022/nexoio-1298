import{describe,expect,it}from'vitest';import{MODULE_DEPENDENCIES,SEGMENT_MODULES}from'@nexoio/core';describe('segment modules',()=>{it('does not enable schedule for restaurants',()=>expect(SEGMENT_MODULES.restaurant).not.toContain('schedule'));it('enables orders for restaurants',()=>expect(SEGMENT_MODULES.restaurant).toContain('orders'));it('uses service orders for technical assistance',()=>expect(SEGMENT_MODULES.technical_assistance).toContain('service_orders'));it('keeps clinical modules separate',()=>expect(SEGMENT_MODULES.clinic).toEqual(expect.arrayContaining(['patients','procedures','professionals'])));it('declares module dependencies',()=>{expect(MODULE_DEPENDENCIES.kitchen).toContain('orders');expect(MODULE_DEPENDENCIES.classes).toContain('plans')})});
