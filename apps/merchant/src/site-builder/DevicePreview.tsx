import { BLOCK_LABELS, DYNAMIC_BLOCKS } from './catalog';
import type { SiteEditor, SitePage } from './types';
export function DevicePreview({editor,page,selected,select}:{editor:SiteEditor;page:SitePage;selected:string|null;select:(id:string)=>void}){return <div className="preview-browser"><div className="preview-bar">{editor.design.showMenu?<><strong>{editor.headline}</strong><nav>{editor.pages.filter(item=>item.enabled).map(item=><span key={item.id}>{item.name}</span>)}</nav></>:null}</div>{page.blocks.filter(item=>item.enabled).map(item=><section className={`preview-block preview-${item.type} ${selected===item.id?'selected':''}`} onClick={()=>select(item.id)} key={item.id}>{item.mediaUrl?<img src={item.mediaUrl} alt=""/>:null}<small>{BLOCK_LABELS[item.type]}</small><h2>{item.title}</h2><p>{item.text||(DYNAMIC_BLOCKS.has(item.type)?'Conteúdo conectado ao sistema e atualizado automaticamente.':'Edite este conteúdo no painel.')}</p>{item.buttonText?<button>{item.buttonText}</button>:null}</section>)}{editor.design.showFooter?<footer>{editor.headline} · {editor.address||'Endereço e horários'}</footer>:null}</div>}
