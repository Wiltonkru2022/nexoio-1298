import { createContext,useCallback,useContext,useEffect,useMemo,useState } from 'react';import type{PropsWithChildren}from'react';import{api,ApiError}from'../lib/api';
export type Membership={businessId:string;businessName:string;segment:string;status:string;roleId:string;roleCode:string};export type SessionContextValue={loading:boolean;user:null|{id:string;name:string;email:string};memberships:Membership[];activeBusiness:Membership|null;permissions:Set<string>;modules:Set<string>;onboardingRequired:boolean;error:string|null;refresh:()=>Promise<void>;selectBusiness:(id:string)=>Promise<void>;logout:()=>Promise<void>};
const SessionContext=createContext<SessionContextValue|null>(null);export function SessionProvider({children}:PropsWithChildren){const[state,setState]=useState<Omit<SessionContextValue,'refresh'|'selectBusiness'|'logout'>>({loading:true,user:null,memberships:[],activeBusiness:null,permissions:new Set(),modules:new Set(),onboardingRequired:false,error:null});const refresh=useCallback(async()=>{setState(x=>({...x,loading:true,error:null}));try{const response=await api.get<{data:{user:{id:string;name:string;email:string};memberships:Membership[];activeBusiness:Membership|null;permissions:string[];modules:string[];onboardingRequired:boolean}}>('/api/v1/platform/context');setState({loading:false,user:response.data.user,memberships:response.data.memberships,activeBusiness:response.data.activeBusiness,permissions:new Set(response.data.permissions),modules:new Set(response.data.modules),onboardingRequired:response.data.onboardingRequired,error:null})}catch(error){setState(x=>({...x,loading:false,user:null,error:error instanceof ApiError?error.message:'Falha ao carregar sessão'}))}},[]);useEffect(()=>{void refresh()},[refresh]);const selectBusiness=useCallback(async(id:string)=>{await api.post('/api/v1/platform/businesses/select',{businessId:id});await refresh()},[refresh]);const logout=useCallback(async()=>{await api.post('/api/auth/sign-out');location.assign('/login')},[]);const value=useMemo(()=>({...state,refresh,selectBusiness,logout}),[state,refresh,selectBusiness,logout]);return <SessionContext.Provider value={value}>{children}</SessionContext.Provider>}export function useSession(){const value=useContext(SessionContext);if(!value)throw new Error('useSession must be used inside SessionProvider');return value}
