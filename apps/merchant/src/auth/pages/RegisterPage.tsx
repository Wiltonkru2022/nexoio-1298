import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,FormField,PasswordField,PasswordStrength}from'../components';import{api,ApiError}from'../../lib/api';export function RegisterPage(){const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);const[message,setMessage]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);const d=new FormData(e.currentTarget);const callbackURL=`${location.origin}/login?verified=1`;try{await api.post('/api/auth/sign-up/email',{name:d.get('name'),email:d.get('email'),password,callbackURL});location.assign(`/verificar-email?email=${encodeURIComponent(String(d.get('email')))}`)}catch(x){setMessage(x instanceof ApiError?x.message:'Não foi possível criar a conta')}finally{setLoading(false)}};return <AuthLayout><AuthCard title="Criar sua conta" description="Comece com uma conta segura; depois configuraremos sua empresa." footer={<span>Já possui conta? <a href="/login">Entrar</a></span>}><AuthForm onSubmit={submit} submitLabel="Criar conta" loading={loading}>{message?<AuthNotice tone="error">{message}</AuthNotice>:null}<FormField label="Nome completo" name="name" required/><FormField label="E-mail" name="email" type="email" required/><PasswordField aria-label="Senha (mínimo 12 caracteres)" name="password" value={password} onChange={e=>setPassword(e.target.value)} required/><PasswordStrength password={password}/></AuthForm></AuthCard></AuthLayout>}
