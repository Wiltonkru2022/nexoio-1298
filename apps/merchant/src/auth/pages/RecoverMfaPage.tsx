import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,FormField}from'../components';import{api,ApiError}from'../../lib/api';export function RecoverMfaPage(){const[error,setError]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();try{await api.post('/api/auth/two-factor/verify-backup-code',{code:new FormData(e.currentTarget).get('code')});location.assign('/')}catch(x){setError(x instanceof ApiError?x.message:'Código inválido')}};return <AuthLayout><AuthCard title="Recuperar MFA" description="Use um dos códigos de recuperação guardados na ativação."><AuthForm onSubmit={submit} submitLabel="Recuperar acesso">{error?<AuthNotice tone="error">{error}</AuthNotice>:null}<FormField label="Código de recuperação" name="code" required/></AuthForm></AuthCard></AuthLayout>}
