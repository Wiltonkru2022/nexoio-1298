import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,FormField}from'../components';import{api}from'../../lib/api';export function ForgotPasswordPage(){const[sent,setSent]=useState(false);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const email=new FormData(e.currentTarget).get('email');await api.post('/api/auth/request-password-reset',{email,redirectTo:`${location.origin}/redefinir-senha`}).catch(()=>undefined);setSent(true)};return <AuthLayout><AuthCard title="Recuperar acesso" description="Enviaremos um link se o e-mail estiver cadastrado." footer={<a href="/login">Voltar ao login</a>}>{sent?<AuthNotice tone="success">Confira sua caixa de entrada e spam.</AuthNotice>:<AuthForm onSubmit={submit} submitLabel="Enviar link"><FormField label="E-mail" name="email" type="email" required/></AuthForm>}</AuthCard></AuthLayout>}
