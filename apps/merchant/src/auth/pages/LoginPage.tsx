import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,FormField,PasswordField}from'../components';import{api,ApiError}from'../../lib/api';export function LoginPage(){const[loading,setLoading]=useState(false);const[error,setError]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setError('');const d=new FormData(e.currentTarget);try{const result=await api.post<{twoFactorRedirect?:boolean}>('/api/auth/sign-in/email',{email:d.get('email'),password:d.get('password'),callbackURL:'/'});location.assign(result.twoFactorRedirect?'/mfa':'/')}catch(x){setError(x instanceof ApiError?x.message:'Login inválido')}finally{setLoading(false)}};return <AuthLayout><AuthCard title="Entrar na Nexoio" description="Use seu e-mail e senha para acessar suas empresas." footer={<span>Novo por aqui? <a href="/cadastro">Criar conta</a></span>}><AuthForm onSubmit={submit} submitLabel="Entrar" loading={loading}>{error?<AuthNotice tone="error">{error}</AuthNotice>:null}<FormField label="E-mail" name="email" type="email" autoComplete="email" required/><PasswordField aria-label="Senha" name="password" autoComplete="current-password" required/><div className="auth-links"><a href="/esqueci-senha">Esqueci minha senha</a><a href="/convite">Tenho um convite</a></div></AuthForm></AuthCard></AuthLayout>}
