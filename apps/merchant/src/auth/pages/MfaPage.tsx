import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,FormField}from'../components';import{api,ApiError}from'../../lib/api';export function MfaPage(){const[error,setError]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();try{await api.post('/api/auth/two-factor/verify-totp',{code:new FormData(e.currentTarget).get('code'),trustDevice:true});location.assign('/')}catch(x){setError(x instanceof ApiError?x.message:'Código inválido')}};return <AuthLayout><AuthCard title="Confirmação em duas etapas" description="Digite o código do seu aplicativo autenticador." footer={<a href="/recuperar-mfa">Usar código de recuperação</a>}><AuthForm onSubmit={submit} submitLabel="Confirmar">{error?<AuthNotice tone="error">{error}</AuthNotice>:null}<FormField label="Código de 6 dígitos" name="code" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required autoFocus/></AuthForm></AuthCard></AuthLayout>}
