import{AuthCard,AuthLayout,AuthNotice}from'../components';import{useSession}from'../SessionProvider';export function SelectBusinessPage(){const{memberships,selectBusiness,error}=useSession();return <AuthLayout><AuthCard title="Escolha a empresa" description="Seu acesso e seus módulos mudam conforme a empresa ativa.">{error?<AuthNotice tone="error">{error}</AuthNotice>:null}<div className="business-list">{memberships.map(x=><button className="business-option" key={x.businessId} onClick={()=>void selectBusiness(x.businessId).then(()=>location.assign('/'))}><span><strong>{x.businessName}</strong><small>{x.roleCode}</small></span><span>Entrar →</span></button>)}</div></AuthCard></AuthLayout>}
