import{useState}from'react';import{AuthCard,AuthForm,AuthLayout,AuthNotice,PasswordField,PasswordStrength}from'../components';import{api,ApiError}from'../../lib/api';export function ResetPasswordPage(){const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const token=new URLSearchParams(location.search).get('token');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();try{await api.post('/api/auth/reset-password',{newPassword:password,token});setMsg('Senha alterada. Você já pode entrar.')}catch(x){setMsg(x instanceof ApiError?x.message:'Link inválido')}};return <AuthLayout><AuthCard title="Definir nova senha" description="Use uma senha forte e exclusiva." footer={<a href="/login">Ir para o login</a>}><AuthForm onSubmit={submit} submitLabel="Redefinir senha">{msg?<AuthNotice tone={msg.startsWith('Senha')?'success':'error'}>{msg}</AuthNotice>:null}<PasswordField aria-label="Nova senha" value={password} onChange={e=>setPassword(e.target.value)} required/><PasswordStrength password={password}/></AuthForm></AuthCard></AuthLayout>}
