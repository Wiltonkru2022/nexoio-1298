import{Button}from'@nexoio/ui';import{AuthCard,AuthLayout,AuthNotice}from'../../auth/components';import type{StepProps}from'../types';export function FinishPage({data,next,back}:StepProps){return <AuthLayout><AuthCard title="Tudo pronto para criar" description="Revise as escolhas e conclua a configuração."><AuthNotice tone="success"><strong>{data.displayName}</strong><br/>{data.segment} · {(data.moduleKeys.length?data.moduleKeys:[]).length} módulos personalizados</AuthNotice><div className="actions"><Button ghost onClick={back}>Voltar</Button><Button onClick={next}>Criar empresa</Button></div></AuthCard></AuthLayout>}
