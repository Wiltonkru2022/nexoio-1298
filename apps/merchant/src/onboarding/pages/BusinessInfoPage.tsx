import{Button}from'@nexoio/ui';import{AuthCard,AuthLayout,FormField}from'../../auth/components';import type{StepProps}from'../types';export function BusinessInfoPage({data,update,next,back}:StepProps){return <AuthLayout><AuthCard title="Dados da empresa" description="Essas informações identificam seu tenant e site público."><div className="auth-form"><FormField label="Nome fantasia" value={data.displayName} onChange={e=>update({displayName:e.target.value})}/><FormField label="Slug público" value={data.slug} onChange={e=>update({slug:e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,'')})}/><FormField label="Telefone" value={data.phone} onChange={e=>update({phone:e.target.value})}/><FormField label="Cidade" value={data.city} onChange={e=>update({city:e.target.value})}/><FormField label="Estado (UF)" maxLength={2} value={data.state} onChange={e=>update({state:e.target.value.toUpperCase()})}/></div><div className="actions"><Button ghost onClick={back}>Voltar</Button><Button disabled={!data.displayName||!data.slug||!data.city||data.state.length!==2} onClick={next}>Continuar</Button></div></AuthCard></AuthLayout>}
