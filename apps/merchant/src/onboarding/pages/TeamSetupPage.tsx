import{Button}from'@nexoio/ui';import{AuthCard,AuthLayout,FormField}from'../../auth/components';import type{StepProps}from'../types';export function TeamSetupPage({data,update,next,back}:StepProps){return <AuthLayout><AuthCard title="Monte sua equipe" description="Opcional: você poderá enviar o convite depois de concluir."><FormField label="E-mail de um membro" type="email" value={data.inviteEmail} onChange={e=>update({inviteEmail:e.target.value})}/><div className="actions"><Button ghost onClick={back}>Voltar</Button><Button onClick={next}>{data.inviteEmail?'Continuar':'Pular por agora'}</Button></div></AuthCard></AuthLayout>}
