import { useCallback, useEffect, useMemo, useState } from 'react';
import { Button, EmptyState, Pill } from '@nexoio/ui';
import { ContentCard } from '../components/ContentCard';
import { Field, Modal } from '../components/Modal';
import { PageHeader } from '../components/PageHeader';
import { StatGrid } from '../components/StatGrid';
import { api, ApiError } from '../lib/api';

type Customer={id:string;name:string;phone?:string|null;whatsapp?:string|null;email?:string|null;cpf?:string|null;birthDate?:string|null;birth_date?:string|null;notes?:string|null;createdAt?:string;created_at?:string};
const createdAt=(x:Customer)=>x.createdAt??x.created_at??'';
const birthDate=(x:Customer)=>x.birthDate??x.birth_date??'';

export function CustomersPage(){
  const[items,setItems]=useState<Customer[]>([]);const[open,setOpen]=useState(false);const[query,setQuery]=useState('');const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState('');
  const load=useCallback(async(force=false)=>{setLoading(items.length===0);setError('');try{const response=await api.get<{data:Customer[]}>('/api/v1/customers?limit=100',{force});setItems(response.data)}catch(reason){setError(reason instanceof ApiError?reason.message:'Não foi possível carregar os clientes.')}finally{setLoading(false)}},[items.length]);
  useEffect(()=>{void load()},[load]);
  const filtered=useMemo(()=>{const q=query.trim().toLocaleLowerCase('pt-BR');return !q?items:items.filter(x=>`${x.name} ${x.phone??''} ${x.whatsapp??''} ${x.email??''} ${x.cpf??''}`.toLocaleLowerCase('pt-BR').includes(q))},[items,query]);
  const month=new Date().getMonth()+1;const birthdays=items.filter(x=>{const value=birthDate(x);return value?Number(value.slice(5,7))===month:false}).length;const since=new Date();since.setDate(1);since.setHours(0,0,0,0);const newMonth=items.filter(x=>{const d=new Date(createdAt(x));return Number.isFinite(d.getTime())&&d>=since}).length;
  const create=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);setSaving(true);setError('');try{await api.post('/api/v1/customers',{name:String(f.get('name')??'').trim(),phone:String(f.get('phone')??'').trim()||undefined,whatsapp:String(f.get('whatsapp')??'').trim()||undefined,email:String(f.get('email')??'').trim()||undefined,cpf:String(f.get('cpf')??'').trim()||undefined,birthDate:String(f.get('birthDate')??'')||undefined,notes:String(f.get('notes')??'').trim()||undefined});setOpen(false);await load(true)}catch(reason){setError(reason instanceof ApiError?reason.message:'Não foi possível criar o cliente.')}finally{setSaving(false)}};
  return <><PageHeader title="Clientes" description="Uma única base de clientes para vendas, agenda, pedidos e demais módulos." action={<Button onClick={()=>setOpen(true)}>Novo cliente</Button>}/><StatGrid items={[{label:'Clientes cadastrados',value:String(items.length)},{label:'Novos no mês',value:String(newMonth)},{label:'Com WhatsApp',value:String(items.filter(x=>Boolean(x.whatsapp||x.phone)).length)},{label:'Aniversariantes do mês',value:String(birthdays)}]}/>{error?<div className="auth-notice error" role="alert">{error} <button onClick={()=>void load(true)}>Tentar novamente</button></div>:null}<ContentCard title="Base de clientes" description="Dados persistidos no Neon e compartilhados pelos fluxos operacionais." action={<div className="toolbar"><input aria-label="Buscar cliente" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nome, telefone, e-mail ou CPF"/><button onClick={()=>void load(true)}>Atualizar</button></div>}>{loading?<div className="empty-state"><h3>Carregando clientes…</h3></div>:filtered.length?<div className="table-shell"><table><thead><tr><th>Cliente</th><th>Contato</th><th>CPF</th><th>Cadastro</th><th>Status</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><strong>{x.name}</strong><br/><small>{x.email||'Sem e-mail'}</small></td><td>{x.whatsapp||x.phone||'—'}</td><td>{x.cpf||'—'}</td><td>{createdAt(x)?new Date(createdAt(x)).toLocaleDateString('pt-BR'):'—'}</td><td><Pill tone="success">Ativo</Pill></td></tr>)}</tbody></table></div>:<EmptyState title="Nenhum cliente encontrado" description="Crie o primeiro cliente ou altere sua busca." action={<Button onClick={()=>setOpen(true)}>Novo cliente</Button>}/>}<div className="card-footer"><Pill>{filtered.length} clientes</Pill></div></ContentCard><Modal open={open} onClose={()=>!saving&&setOpen(false)} onSubmit={create} title="Novo cliente" description="Este cadastro será usado por vendas, agenda, pedidos e demais módulos." submitLabel={saving?'Salvando…':'Salvar cliente'}><Field label="Nome completo" name="name" required/><Field label="Telefone" name="phone" type="tel"/><Field label="WhatsApp" name="whatsapp" type="tel"/><Field label="E-mail" name="email" type="email"/><Field label="CPF" name="cpf"/><Field label="Data de nascimento" name="birthDate" type="date"/><Field label="Observações" name="notes"/></Modal></>}
