import{adminApi}from'../../lib/api';import{AdminAuthForm,AdminAuthLayout,AdminField}from'../AuthLayout';export function AdminRecoverMfaPage(){const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();await adminApi.post('/api/auth/two-factor/verify-backup-code',{code:new FormData(e.currentTarget).get('code')});location.assign('/')};return <AdminAuthLayout title="Recuperar MFA" description="Use um recovery code de uso único."><AdminAuthForm onSubmit={submit} label="Recuperar"><AdminField label="Recovery code" name="code" required/></AdminAuthForm></AdminAuthLayout>}
