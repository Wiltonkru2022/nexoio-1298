import{useState}from'react';import{adminApi}from'../../lib/api';import{AdminAuthForm,AdminAuthLayout,AdminField}from'../AuthLayout';export function AdminMfaPage(){const[error,setError]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();try{await adminApi.post('/api/auth/two-factor/verify-totp',{code:new FormData(e.currentTarget).get('code')});location.assign('/')}catch{setError('Código inválido ou expirado')}};return <AdminAuthLayout title="MFA obrigatório" description="Confirme o código do autenticador para acessar o Master Admin.">{error?<div className="auth-notice error">{error}</div>:null}<AdminAuthForm onSubmit={submit} label="Confirmar"><AdminField label="Código" name="code" inputMode="numeric" maxLength={6} required/></AdminAuthForm><a href="/recuperar-mfa">Usar recovery code</a></AdminAuthLayout>}
