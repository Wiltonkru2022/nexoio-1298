import{useState}from'react';import{AdminApiError,adminApi}from'../../lib/api';import{AdminAuthForm,AdminAuthLayout,AdminField}from'../AuthLayout';export function AdminLoginPage(){const[error,setError]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const d=new FormData(e.currentTarget);try{const r=await adminApi.post<{twoFactorRedirect?:boolean}>('/api/auth/sign-in/email',{email:d.get('email'),password:d.get('password')});location.assign(r.twoFactorRedirect?'/mfa':'/')}catch(x){setError(x instanceof AdminApiError?x.message:'Login inválido')}};return <AdminAuthLayout title="Acesso Master" description="Exclusivo para administradores da plataforma.">{error?<div className="auth-notice error">{error}</div>:null}<AdminAuthForm onSubmit={submit} label="Entrar com segurança"><AdminField label="E-mail administrativo" name="email" type="email" required/><AdminField label="Senha" name="password" type="password" minLength={12} required/></AdminAuthForm></AdminAuthLayout>}
